[project]
name = "bioindex"
version = "0.0.1"
description = "ACMG inverted index"
requires-python = ">=3.14"

dependencies = [
    "sqlalchemy>=2.0"
]

[tool.setuptools.packages.find]
where = ["."]
include = ["db", "data", "parsers", "scripts", "tests"]
exclude = ["venv*"]